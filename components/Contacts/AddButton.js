import React from 'react';

const AddButton = () => {
    return (
        <div className='add__btn'>
            <button><i class="ri-add-line"></i>Add Contacts</button>
        </div>
    );
}

export default AddButton;
